<%= image_tag "logo.png" %>

<div class="container">
  <h1>Events</h1>

  <ul>
    <% @events.each do |event| %>
      <li><%= link_to event.name, event_path(event) %></li>
    <% end %>
  </ul>
</div>
<%= link_to "New Event", new_event_path %>
